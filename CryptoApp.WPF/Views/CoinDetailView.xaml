<UserControl x:Class="CryptoApp.WPF.Views.CoinDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="White">
             
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Content="← Повернутися до списку" 
                Command="{Binding GoBackCommand}" 
                Width="180" Height="35" 
                HorizontalAlignment="Left" Margin="0,0,0,20"/>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="{Binding Coin.Name}" FontSize="28" FontWeight="Bold" Margin="0,0,10,0"/>
            <TextBlock Text="{Binding Coin.Symbol}" FontSize="28" Foreground="Gray"/>
            
            <Button Content="Оновити ринки" Command="{Binding LoadMarketsCommand}" 
                    Width="120" Height="30" Margin="20,0,0,0" Padding="5,0"/>
            
            <TextBlock Text="{Binding LastUpdatedText}" 
                       VerticalAlignment="Center" Margin="15,0,0,0" 
                       FontWeight="SemiBold" Foreground="#444444"/>
            
            <TextBlock Text="Оновлення даних..." 
                       VerticalAlignment="Center" Margin="20,0,0,0" Foreground="Gray"
                       Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </StackPanel>

        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Markets}" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  RowHeight="35"
                  FontSize="14">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Біржа" Binding="{Binding ExchangeName}" Width="150" FontWeight="Bold"/>
                <DataGridTextColumn Header="Пара" Binding="{Binding Pair}" Width="120"/>
                <DataGridTextColumn Header="Остання ціна" Binding="{Binding PriceUsd, StringFormat=\{0:C6\}}" Width="150"/>
                <DataGridTemplateColumn Header="Посилання на торги" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Margin="5,0">
                                <Hyperlink Command="{Binding DataContext.OpenUrlCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                           CommandParameter="{Binding TradeUrl}">
                                    <Run Text="Відкрити торги"/>
                                </Hyperlink>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn> 
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>